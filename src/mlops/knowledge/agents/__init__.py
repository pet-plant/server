"""Interchangeable probe-generation agents, one package per version.

Pick one through :mod:`mlops.knowledge.registry`; do not import a version
directly from outside ``mlops.knowledge``, or the choice stops being visible in
one place.
"""
